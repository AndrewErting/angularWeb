import { CenterHorizontalDirective } from './center-horizontal.directive';

describe('CenterHorizontalDirective', () => {
  it('should create an instance', () => {
    const directive = new CenterHorizontalDirective();
    expect(directive).toBeTruthy();
  });
});
