import { OpenSidebarDirective } from './open-sidebar.directive';

describe('OpenSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new OpenSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
