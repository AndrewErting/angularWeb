import { BufferMarginDirective } from './buffer-margin.directive';

describe('BufferMarginDirective', () => {
  it('should create an instance', () => {
    const directive = new BufferMarginDirective();
    expect(directive).toBeTruthy();
  });
});
