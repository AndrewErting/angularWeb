import { AutoFullHeightDirective } from './auto-full-height.directive';

describe('AutoFullHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoFullHeightDirective();
    expect(directive).toBeTruthy();
  });
});
